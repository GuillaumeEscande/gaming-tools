use crate::linear_hexagon;
use crate::model;
use crate::constant;
use board::Board;

pub fn sort_better_cases(board: &linear_hexagon::LinearHexagon::<model::Case>) -> Vec<i16>{
    return constant::Constant::case_assembly()[0].clone();
}
